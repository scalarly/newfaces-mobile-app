/**
 * GalleryScreen - Enhanced gallery with modern features
 * 
 * This screen now uses the enhanced gallery implementation with:
 * - Multi-selection and batch operations
 * - PDF export functionality  
 * - File download with progress tracking
 * - Authenticated image requests
 * - Linear gradient support
 * - Modern file system operations
 */

import React from 'react';
import { EnhancedGalleryScreen } from '../features/gallery';

const GalleryScreen: React.FC = () => {
  return <EnhancedGalleryScreen />;
};

export default GalleryScreen;